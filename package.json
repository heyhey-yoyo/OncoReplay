{
  "name": "oncoreplay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An interactive research timeline prototype for oncology topics.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check public/app.js && node --check public/core.mjs && node --check src/worker/index.js && node --check scripts/dev-server.mjs && node --check scripts/build.mjs",
    "deploy": "npm run build && wrangler deploy",
    "db:local": "wrangler d1 migrations apply oncoreplay-db --local",
    "db:remote": "wrangler d1 migrations apply oncoreplay-db --remote",
    "deploy:demo": "npm run build && wrangler deploy --config wrangler.demo.jsonc"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
